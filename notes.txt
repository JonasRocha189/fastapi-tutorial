install fastapi // uv add "fastapi[standard]"
includes uvicorn, cli command, Jinja2 for template

to run fastpi // uv run fastapi dev main.py
dev mode include autorreload and debug

fastapi has two docs
/docs
/redoc

adding the parameter include_in_schema=False, make fastapi not show the route on docs, but is still available if you access it

setup Jinja template and static files

path parameter = parameters that are passed through url

using error handling and fixing routing

--

pydantic is builtin in fastapi, no needing to install
for schemas data validation

create schemas.py

adding response_model=list[PostResponse] on route, makes the fastpi validate based on schema created on pydantic

in docs, will see the validations required for each route using pydantic


---

add sqlalchemy

will use sqlite initially

create database.py

is important to create session to use database

models
set relationship


---

Put // usually updates the entire content
patch // usually updates a part of the content
obs: In practice just uses the patch (partial)

Delete: usually returns 204

Status code 
200 - ok // GET, PUT or PATCH
201 - Created // Successful POST for users and posts
204 - No Content // Successful DELETE
400 - Bad Request // Duplicate username/email when creating user 
404 - Not Found // Resource doesn't exists (user or post)
422 - Unprocessable Entity // Validation error (automatic from Pydantic)


---

async

uv add aiosqlite // to turn database into async
update database configurations

import contextlib in main 

uv add greenlet

---

Routers

create a folder and the modules for the router

router concatenate the path

___

authentication = who are you?
authorization - what are you allowed to do?

install 
"pwdlib[argon2]" // better than bcrypt
pyjwt // for authentication
pydantic-settings // for managing configurations

uv add "pwdlib[argon2]" pyjwt pydantic-settings

is better delete the database for now, since still does not have migrations

Generates e token key from terminal
python -c "import secrets; print(secrets.token_hex(32))"

the schema CurrentUser check if the user is logged in  


----

File upload 
uv add pillow

create image_utils.py

tip: for small project, creating a folder to store images works good, but ideally is better working with S3 or other cloud storage 


-----

pagination

create a new pydantic contract for pagination

obs: fastapi has a package that handles pagination

----

Password Reset 

- send email in background

uv add aiosmtplib // async lib to send email

create reset token expiration

mailtrap // cloud tool for development mode to use email delivery
https://mailtrap.io/