install fastapi // uv add "fastapi[standard]"
includes uvicorn, cli comand, Jinja2 for template

to run fastpi // uv run fastapi dev main.py
dev mode include autorreload and debug

fastapi has two docs
/docs
/redoc

adding the parameter include_in_schema=False, make fastapi not show the route on docs, but is still available if you access it

setup Jinja template and static files

path parameter = parameters that are passed through url

using error handling and fixing routing

--

pydantic is builtin in fastapi, no needing to install
for schemas data validation

create schemas.py

adding response_model=list[PostResponse] on route, makes the fastpi validate based on schema created on pydantic

in docs, will see the validations required for each route using pydantic


---

add sqlalchemy

will use sqlite initially

create database.py

is important to create session to use database

models
set relationship
